---
// src/pages/channel-partner/index.astro
const token = Astro.cookies.get('cp_sid')?.value;
if (token) {
  return Astro.redirect('/channel-partner/dashboard');
} else {
  return Astro.redirect('/channel-partner/login');
}
---
